class Main {

	function void main() {
		var Decoder decoder;
		var Float cos1, cos2, cos3, cos4;

		let decoder = Decoder.new();

		do decoder.Decode();

		let cos1 = Float.new(0, 26);
		let cos1 = cos1.Mult(Float.new(4,0));
		do cos1.print();
		let cos1 = cos1.intMult(2);
		do cos1.print();
		
		return;
	}
}
